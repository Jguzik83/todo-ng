export class Task {
	id: number;
	description: string;
	completed: boolean;
	editable: boolean;
}

// I would like to add a due date at some point, with the functionality to add completed date and time when clicked on finish